apiVersion: v1
kind: Service
metadata:
  name: meme-storage-ocr-service
  namespace: bots
spec:
  selector:
    app: meme-storage-ocr-bot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      